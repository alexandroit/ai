{
  "name": "stackline-ai-workspace",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "check": "pnpm run lint && pnpm run docs:build && pnpm run typecheck && pnpm run test && pnpm run build && pnpm run examples:smoke && pnpm run pack:dry-run",
    "docs:build": "node scripts/validate-docs.mjs",
    "docs:dev": "node -e \"console.log('Docs are Markdown-first in this repository. Open docs/index.md or docs/ai/site/index.html.')\"",
    "docs:preview": "node -e \"console.log('Preview docs by serving docs/ with any static server.')\"",
    "examples:smoke": "node scripts/smoke-examples.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "pack:dry-run": "pnpm -r --if-present pack:dry-run"
  },
  "devDependencies": {
    "@types/node": "^22.19.20",
    "tsup": "^8.5.1",
    "typescript": "6.0.3",
    "vitest": "^4.0.15"
  }
}
