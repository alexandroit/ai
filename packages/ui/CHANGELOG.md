# Changelog

## Unreleased

- Hardens safe HTML rendering against nested dangerous tags, malformed markup,
  and sanitization collisions while preserving the public custom-element API.

## 0.0.5 - 2026-08-20

- Updates the model picker to `@stackline/multiselect@1.1.3`.
- Preserves the custom-element API, language packs, storage format, and rendering behavior from `0.0.4`.
