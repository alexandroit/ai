# SQLite Memory

Demonstrates `@stackline/ai-memory-sqlite` with a fake provider and a real
SQLite/sql.js memory file under `data/`.

```bash
pnpm --filter stackline-ai-example-sqlite-memory smoke
```
