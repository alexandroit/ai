# PostgreSQL RAG

Demonstrates the `@stackline/ai-rag-postgres` contract with a fake PostgreSQL
client for smoke tests. Use `sql/schema.sql` and `sql/seed.sql` as the starting
point for a real database.
