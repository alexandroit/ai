# Complete Stack

Complete backend example with:

- Ollama provider;
- Fetch-compatible HTTP handler;
- optional SQLite memory;
- optional PostgreSQL RAG;
- graceful shutdown.

```bash
cp .env.example .env
pnpm --filter stackline-ai-example-complete-stack start
```
